{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "dependsOn": ["^db:generate"],
      "cache": false,
      "persistent": true
    },
    "db:generate": {
      "inputs": ["prisma/schema.prisma"]
    },
    "lint": {},
    "test": {},
    "typecheck": {
      "dependsOn": ["^db:generate"]
    }
  }
}
